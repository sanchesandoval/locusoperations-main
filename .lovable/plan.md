

# Revert Headline Spacing

Increase the bottom margin on the headline container in `src/pages/Scorecard.tsx` from `mb-2` back to `mb-4`.

## Technical Detail

In `src/pages/Scorecard.tsx`, line 21, change `mb-2` to `mb-4` on the heading wrapper div.

